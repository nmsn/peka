# Pika 设计规范文档

## 概述

本文档为 Pika 颜色取色器应用的完整 UI/UX 设计规范，包含页面布局、颜色系统、排版规范、组件样式、间距规范及图标系统。本规范可直接用于 Electron/React 复刻项目的开发。

---

## 目录

1. [页面整体布局结构](#一页面整体布局结构)
2. [颜色系统](#二颜色系统)
3. [排版规范](#三排版规范)
4. [组件样式](#四组件样式)
5. [间距与边距规范](#五间距与边距规范)
6. [图标系统](#六图标系统)

---

## 一、页面整体布局结构

### 1.1 窗口类型与尺寸

| 窗口类型 | 宽度 | 高度 | 最小尺寸 | 最大尺寸 | 标题栏 |
|---------|------|------|---------|---------|-------|
| 主窗口 | 380px | 150px | 无硬性限制 | 无硬性限制 | 隐藏 |
| 偏好设置 | 750px | 750px | - | - | 显示 |
| 启动画面 | 650px | 380px | - | - | 显示 |
| 关于窗口 | 750px | 650px | - | - | 显示 |

### 1.2 主窗口布局结构

```
┌─────────────────────────────────────────────────────────────────┐
│                      标题栏附件区域                              │
│  [Hex][RGB][HSB][HSL][LAB][OpenGL]                    [⚙️]    │
├─────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────┬─────────────────────────────────────┐│
│  │                       │                                     ││
│  │    前景色区域         │ │    背景色区域                      ││
│  │    (50% 宽度)        │ │    (50% 宽度)                    ││
│  │                       │ │                                     ││
│  │   Foreground         │ │   Background                       ││
│  │   #FF0000           │ │   #000000                          ││
│  │   Red               │ │   Black                            ││
│  │              [📋][🎨]│ │              [📋][🎨]              ││
│  │                       │ │                                     ││
│  └───────────────────────┴─────────────────────────────────────┘│
│                              ↕ 悬停显示                          │
│                          [交换按钮]                              │
├─────────────────────────────────────────────────────────────────┤
│  对比度比率 │ 21:1           WCAG │ ✓ AA  ✓ AAA               │
│  (左侧 50%)│                 (右侧 50%)                        │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 响应式设计规则

| 断点 | 宽度范围 | 布局调整 |
|------|---------|---------|
| 紧凑 | < 350px | 禁用部分功能按钮 |
| 标准 | 350px - 500px | 标准双列布局 |
| 舒适 | > 500px | 双列布局，可调整窗口尺寸 |

### 1.4 布局层级

```css
.app-container {
  display: flex;
  flex-direction: column;
  min-width: 380px;
  min-height: 150px;
}

/* 标题栏 */
.toolbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* 主内容区 */
.content {
  flex: 1;
  position: relative;
  display: flex;
}

/* 颜色选择器容器 */
.color-pickers {
  display: flex;
  flex: 1;
}

/* 底部栏 */
.footer {
  display: flex;
  align-items: center;
  height: 50px;
}
```

---

## 二、颜色系统

### 2.1 系统颜色

使用 macOS 系统颜色，随明暗模式自动切换：

| 颜色名称 | 用途 | 浅色模式 | 深色模式 |
|---------|------|---------|---------|
| `primary` | 主文本 | #000000 | #FFFFFF |
| `secondary` | 次要文本 | #3C3C43 (60%) | #EBEBF5 (60%) |
| `tertiary` | 辅助文本 | #3C3C43 (30%) | #EBEBF5 (30%) |
| `divider` | 分割线 | #3C3C43 (29%) | #545458 (65%) |
| `background` | 背景 | System | System |

### 2.2 应用主题色

| 用途 | 颜色代码 | 说明 |
|------|---------|------|
| 强调色 | System Blue | `#007AFF` (macOS) |
| 成功/合规 | System Green | `#34C759` |
| 错误/警告 | System Red | `#FF3B30` |

### 2.3 功能色

| 状态 | 颜色 | 用途 |
|------|------|------|
| 合规通过 | primary | WCAG/APCA 合规状态 |
| 不合规 | secondary | 未达标准提示 |

### 2.4 颜色按钮背景

| 模式 | 背景色 | 色值 |
|------|-------|------|
| 浅色模式 | 交换按钮背景 | `#E9E9E9` |
| 深色模式 | 交换按钮背景 | `#1B1B1B` |

### 2.5 取色悬浮窗颜色

| 元素 | 样式 |
|------|------|
| 背景 | 取色颜色本身 |
| 文字颜色 | 自动计算对比色（黑/白） |
| 边框 | 颜色本身 20% 透明度 |
| 阴影 | 白色径向渐变 (30% → 透明) |

---

## 三、排版规范

### 3.1 字体家族

| 用途 | 字体 | 回退 |
|------|------|------|
| 正文 | SF Pro | -system-ui, -apple-system |
| 数值 | SF Mono | Menlo, Monaco |
| 等宽 | SF Pro Rounded | - |

### 3.2 字号规范

| 元素 | 字号 | 字重 | 行高 |
|------|------|------|------|
| 窗口标题 | 13pt | Regular | - |
| 区块标题 | 16pt | Semibold | - |
| 正文 | 13pt | Regular | - |
| 颜色值 (大) | 18pt | Regular | - |
| 标签 | 12pt | Semibold | - |
| Caption | 11pt | Regular | - |
| 小标签 | 10pt | Regular | - |
| 悬停提示 | 12pt | Regular | - |

### 3.3 字号应用场景

| 字号 | 应用场景 |
|------|---------|
| 18px | 颜色值显示 (#FF0000) |
| 16pt | 设置页面区块标题 |
| 14pt | 快捷键按钮 |
| 12px | 标签、颜色名称、悬停按钮文字 |
| 11px | Footer 标签、辅助说明 |
| 10px | 辅助标签 (如 "LG") |

### 3.4 字重规范

| 字重 | 值 | 应用 |
|------|-----|------|
| Regular | 400 | 正文、颜色值 |
| Medium | 500 | 颜色名称 |
| Semibold | 600 | 标签、Caption |
| Bold | 700 | 无使用 |

---

## 四、组件样式

### 4.1 按钮组件

#### 4.1.1 EyedropperButton (颜色按钮)

**结构**:
```
┌─────────────────────────────┐
│  类型标签 (Foreground)      │
│                             │
│  #FF0000                   │
│                             │
│  Red                       │
│      [复制] [取色]          │
└─────────────────────────────┘
```

**尺寸**: 父容器宽度 50%，高度自适应

**内边距**: 10px 全方向

**状态**:

| 状态 | 样式 |
|------|------|
| 默认 | 背景 = 颜色本身 |
| 按下 | 透明度 = 0.8 |
| 动画 | easeIn 0.15s |

#### 4.1.2 悬停按钮 (复制/取色)

**尺寸**: 14x14px

**内边距**: 8px 水平, 8px 垂直

**圆角**: 100px (胶囊形)

**状态**:

| 状态 | 样式 |
|------|------|
| 默认 | 隐藏 (opacity: 0) |
| 悬停 | 显示 (opacity: 1) |
| 按下 | 透明度 0.8 |

**动画**:
- 显示: easeInOut
- 出现延迟: 0.1s

#### 4.1.3 交换按钮

**尺寸**: 高度 32px

**位置**: 右上角，padding 16px

**圆角**: 100px (胶囊形)

**状态**:

| 状态 | 样式 |
|------|------|
| 默认 | 隐藏 (opacity: 0) |
| 悬停 ColorPickers | 显示 (opacity: 1) |
| 点击 | 旋转 180° + 透明度 0.8 |

**动画**: easeInOut

### 4.2 分隔线

**样式**: 1px 水平线

**颜色**:
- 浅色模式: `rgba(60, 60, 67, 0.29)`
- 深色模式: `rgba(84, 84, 88, 0.65)`

### 4.3 颜色格式选择器

**类型**: Segmented Picker (分段选择器)

**选项**: Hex, RGB, HSB, HSL, LAB, OpenGL

**样式**:
- 等分宽度
- 圆角: 6px
- 选中项: 强调色背景

### 4.4 设置菜单按钮

**图标**: gearshape

**尺寸**: 14x14px (图标本身)

**样式**: BorderlessButtonMenuStyle

### 4.5 合规状态组件

#### 4.5.1 ComplianceToggle

**结构**:
```
[✓/✗] [标签]
```

**尺寸**:
- Full 模式: 图标 14x14px
- Small 模式: 图标 13x13px

**状态**:

| 状态 | 图标 | 颜色 |
|------|------|------|
| 合规 | checkmark.circle.fill | primary |
| 不合规 | xmark.circle | secondary |

#### 4.5.2 ComplianceToggleGroup

**WCAG 模式**:

```
Weight 主题:
Normal:     [✓ AA] [✓ AAA]
Large Text: [✓ AA] [✓ AAA]

Contrast 主题:
[✓ AA 3:1] [✓ AA/AAA 4.5:1] [✓ AAA 7:1]
```

**APCA 模式**:
```
[✓ Baseline Lc≥30] [✓ Headline Lc≥45] [✓ Title Lc≥60] [✓ Body Lc≥75]
```

### 4.6 底部栏 (Footer)

**高度**: 50px

**内边距**: 12px 水平

**布局**: HStack，间距 16px

**背景**: 毛玻璃效果
```css
backdrop-filter: blur(20px);
background: rgba(255, 255, 255, 0.5); /* 浅色 */
background: rgba(0, 0, 0, 0.5);     /* 深色 */
```

---

## 五、间距与边距规范

### 5.1 间距系统

| 间距值 | 用途 |
|--------|------|
| 0px | VStack 内部紧排列 |
| 2px | 按钮内部元素、比率数字间距 |
| 3px | Footer 右侧 VStack 内部 |
| 4px | 无使用 |
| 6px | 颜色按钮内部 |
| 8px | 悬停按钮内边距 |
| 10px | EyedropperButton 内边距 |
| 12px | Footer 内边距 |
| 16px | HStack 主间距、交换按钮位置 |
| 20px | 偏好设置内部 |

### 5.2 边距规范

| 元素 | 边距 |
|------|------|
| 主窗口内容 | 系统默认 |
| EyedropperButton | 10px |
| Footer 水平 | 12px |
| 交换按钮 | 16px (top, right) |
| NavigationMenu | 5px left, 10px right |

### 5.3 间距速查表

```css
/* 间距变量 */
:root {
  --space-xs: 2px;
  --space-sm: 6px;
  --space-md: 10px;
  --space-lg: 12px;
  --space-xl: 16px;
  --space-2xl: 20px;

  /* 组件特定 */
  --button-padding: 10px;
  --hover-button-padding: 8px;
  --footer-height: 50px;
  --toolbar-height: auto;
}
```

---

## 六、图标系统

### 6.1 图标来源

**图标库**: SF Symbols (Apple 系统图标)

**项目内图标**: SVG 格式，位于 `Assets.xcassets/Icons/`

### 6.2 项目图标清单

| 图标名称 | 文件 | 尺寸 | 用途 |
|---------|------|------|------|
| arrow.triangle.swap | arrow.triangle.swap.svg | 14x14 | 交换颜色 |
| doc.on.doc | doc.on.doc.svg | 14x14 | 复制 |
| paintpalette | paintpalette.svg | 14x14 | 系统取色器 |
| gearshape | gearshape.svg | 14x14 | 设置 |
| checkmark.circle.fill | checkmark.circle.fill.svg | 14x14 | 合规通过 |
| xmark.circle | xmark.circle.svg | 14x14 | 不合规 |
| eyedropper | eyedropper.svg | - | 未使用 |
| arrow.down.circle | arrow.down.circle.svg | - | 未使用 |
| circle.lefthalf.fill | circle.lefthalf.fill.svg | - | 未使用 |
| ellipsis.circle | ellipsis.circle.svg | - | 未使用 |
| hand.thumbsup.fill | hand.thumbsup.fill.svg | - | 未使用 |
| storefront.circle | storefront.circle.svg | - | 未使用 |

### 6.3 尺寸规范

| 用途 | 尺寸 |
|------|------|
| 工具栏图标 | 14x14px |
| 复制/取色按钮 | 14x14px |
| 交换按钮 | 14x14px |
| 合规状态图标 (Full) | 14x14px |
| 合规状态图标 (Small) | 13x13px |

### 6.4 图标使用规则

1. **颜色**: 跟随当前主题 (primary/secondary)
2. **对齐**: 居中对齐
3. **间距**: 与文本间距 2px

### 6.5 图标代码示例

```tsx
// React 组件中使用
import { ReactComponent as SwapIcon } from './icons/arrow.triangle.swap.svg';
import { ReactComponent as CopyIcon } from './icons/doc.on.doc.svg';
import { ReactComponent as PaletteIcon } from './icons/paintpalette.svg';

// 或使用 Lucide React (推荐替代方案)
import { ArrowUpDown, Copy, Palette, Settings, Check, X } from 'lucide-react';

// SVG 内联使用
<svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg">
  <path d="..." fill="currentColor"/>
</svg>
```

### 6.6 SF Symbols 对应表

| SF Symbols | 用途 |
|------------|------|
| arrow.triangle.swap | 交换 |
| doc.on.doc | 复制 |
| paintpalette | 取色器 |
| gearshape | 设置 |
| checkmark.circle.fill | 合规 |
| xmark.circle | 不合规 |

---

## 附录

### A. CSS 变量定义

```css
:root {
  /* 颜色 */
  --color-primary: #000000;
  --color-secondary: rgba(60, 60, 67, 0.6);
  --color-divider: rgba(60, 60, 67, 0.29);
  --color-success: #34C759;
  --color-error: #FF3B30;
  --color-accent: #007AFF;

  /* 浅色模式按钮 */
  --button-bg-light: #E9E9E9;
  /* 深色模式按钮 */
  --button-bg-dark: #1B1B1B;

  /* 字体 */
  --font-sans: -apple-system, BlinkMacSystemFont, 'SF Pro', sans-serif;
  --font-mono: 'SF Mono', Menlo, Monaco, monospace;

  /* 字号 */
  --text-xl: 18px;
  --text-lg: 16px;
  --text-md: 13px;
  --text-sm: 12px;
  --text-xs: 11px;
  --text-xxs: 10px;

  /* 间距 */
  --space-xs: 2px;
  --space-sm: 6px;
  --space-md: 10px;
  --space-lg: 12px;
  --space-xl: 16px;
  --space-2xl: 20px;

  /* 组件 */
  --footer-height: 50px;
  --icon-size: 14px;
  --border-radius: 100px;
}
```

### B. 动画速查

| 动画 | 时长 | 缓动 |
|------|------|------|
| 颜色变化 | 0.15s | easeIn |
| 按钮显示/隐藏 | - | easeInOut |
| 交换旋转 | - | easeInOut |
| 悬停按钮延迟 | 0.1s | - |
| 悬浮窗淡入 | 0.2s | easeOut |
| 悬浮窗淡出 | 0.2s | easeIn |

---

## 相关文档

- [Footer 组件实现指南](./FOOTER_IMPLEMENTATION.md)
- [取色功能触发链分析](./PICKER_TRIGGER_ANALYSIS.md)
- [项目分析文档](./PIKA_ANALYSIS.md)
