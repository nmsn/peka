# Peka 项目重构计划：Electron → Electrobun（PoC 优先版）

## 一、结论先行

这次迁移不应按“直接重构现有项目”推进，而应按“先做可行性验证，再决定是否迁移”推进。

原因很明确：Peka 不是普通的桌面壳应用，而是一个高度依赖桌面运行时能力的取色工具。当前产品的核心体验依赖以下能力：

- 屏幕取色与像素读取
- 全局快捷键
- 系统托盘与 menubar 模式
- 无边框窗口与自定义标题栏
- 主进程向渲染层推送窗口状态和快捷键事件
- 本地设置持久化

这些能力在 Electron 中都有成熟实现，但在 Electrobun 上是否能稳定、完整、跨平台地落地，目前还不能假定成立。

因此，本方案调整为：

- 先完成 `阶段 0：可行性验证（PoC）`
- 只有在核心能力验证通过后，才进入正式迁移
- 如果 PoC 不能证明关键链路可行，则终止迁移，保留 Electron 方案

## 二、迁移动机与边界

### 2.1 迁移动机

迁移到 Electrobun 的主要目标：

- 降低安装包体积
- 缩短冷启动时间
- 减少运行时内存占用
- 简化部分运行时分发链路

### 2.2 本次计划的边界

本计划不是“先写完整实现，再边做边验证”，而是分为两个阶段：

1. **PoC 阶段**：只验证迁移是否成立
2. **正式迁移阶段**：在 PoC 通过后，才迁移现有功能

### 2.3 Go / No-Go 原则

只要下面任一项不能稳定成立，本次迁移应暂停或终止：

- 屏幕取色主链路不可用
- 主进程与 WebView 间通信无法满足当前事件模型
- 托盘 / menubar 模式无法满足现有交互
- 全局快捷键无法实现，且无法接受产品降级
- 打包、签名、公证链路无法形成可发布流程

## 三、当前项目功能矩阵

在决定迁移前，必须先把“当前 Electron 项目到底已经依赖了什么能力”写清楚。

### 3.1 已有功能与迁移优先级

| 功能 | 当前状态 | 优先级 | 是否允许降级 | 备注 |
| --- | --- | --- | --- | --- |
| 前景色取色 | 已实现 | P0 | 否 | 核心功能 |
| 背景色取色 | 已实现 | P0 | 否 | 核心功能 |
| 颜色复制 | 已实现 | P0 | 否 | 核心功能 |
| 颜色格式切换 | 已实现 | P1 | 否 | 影响主流程 |
| 辅助功能对比面板 | 已实现 | P1 | 否 | 现有 UI 一部分 |
| 设置持久化 | 已实现 | P0 | 否 | 当前依赖 `electron-store` |
| 自定义标题栏 | 已实现 | P1 | 是 | 可接受轻微 UI 调整 |
| 无边框窗口控制 | 已实现 | P0 | 否 | 最小化 / 最大化 / 关闭 |
| 主进程推送窗口事件 | 已实现 | P0 | 否 | 如 `window:maximized` |
| 菜单栏 / 托盘模式 | 已实现 | P0 | 否 | 产品使用方式之一 |
| 全局快捷键 | 已实现 | P0 | 视产品决定 | 如 `Cmd+D` |
| 置顶 / 隐藏任务栏 | 已实现 | P1 | 是 | 与 menubar 模式相关 |
| 自动更新 | 未明确完成度 | P2 | 是 | 可延后 |
| 多窗口设置页 | 未最终确定 | P2 | 是 | 可改为单窗口内面板 |

### 3.2 建议的迁移目标分级

- `P0 必须保留`：取色、复制、设置持久化、窗口控制、托盘 / menubar、主进程事件、基础通信链路
- `P1 尽量保留`：自定义标题栏、格式切换体验、辅助功能面板、窗口置顶逻辑
- `P2 可延期`：独立设置窗口、自动更新、Linux 全量支持

## 四、技术栈对比（带验证状态）

下表中的“候选替代”不代表已经成立，只表示迁移方向。所有未实际验证过的能力都必须标记状态。

| 能力 | 当前 Electron 方案 | Electrobun 候选方案 | 验证状态 |
| --- | --- | --- | --- |
| 主窗口 | `BrowserWindow` | `BrowserWindow` | 需 PoC 验证 |
| 渲染层承载 | Chromium | 系统 WebView | 需 PoC 验证 |
| 双向通信 | `ipcMain` / `ipcRenderer` / preload | RPC / bridge | 需 PoC 验证 |
| 主进程推送事件 | `webContents.send` | 自定义 event bridge | 需 PoC 验证 |
| 托盘 | `Tray` | `Tray` 或平台封装 | 需 PoC 验证 |
| 全局快捷键 | `globalShortcut` | 原生绑定或 Electrobun 能力 | 需 PoC 验证 |
| 剪贴板 | `clipboard` | Bun / 平台 API | 需 PoC 验证 |
| 本地设置 | `electron-store` | JSON 文件 + schema 迁移 | 可实现 |
| 自动更新 | `electron-updater` | 待选方案 | 暂无明确方案 |
| 原生取色 | Electron + 当前主进程模块 | 原生绑定 | 需 PoC 验证 |
| 打包分发 | Electron builder / 现有链路 | Electrobun 打包链路 | 需 PoC 验证 |

## 五、当前架构与目标架构

### 5.1 当前 Electron 架构

```text
Electron:
┌──────────────────────┐
│ Main Process         │
│ - src/main/index.ts  │
│ - modules/ipc.ts     │
│ - modules/store.ts   │
│ - modules/eyedropper │
└──────────┬───────────┘
           │
           │ IPC + events
           │
┌──────────▼───────────┐
│ Preload Bridge       │
│ - src/preload/index  │
└──────────┬───────────┘
           │
           │ safe API bridge
           │
┌──────────▼───────────┐
│ Renderer             │
│ - React App          │
│ - TitleBar           │
│ - ColorDisplay       │
│ - AccessibilityPanel │
└──────────────────────┘
```

### 5.2 目标 Electrobun 架构

目标架构不是单纯“IPC 改 RPC”，而是要保留三类通信：

- 请求响应：如 `pickColor()`、`getSettings()`
- 主进程事件推送：如窗口最大化状态变化
- 应用命令分发：如快捷键触发取色、复制、打开设置

```text
Electrobun:
┌──────────────────────────┐
│ Bun Main Runtime         │
│ - app bootstrap          │
│ - window manager         │
│ - tray manager           │
│ - settings store         │
│ - native color module    │
│ - app event bridge       │
└────────────┬─────────────┘
             │
             │ RPC + event bridge
             │
┌────────────▼─────────────┐
│ WebView                  │
│ - React App              │
│ - UI state               │
│ - window API adapter     │
└──────────────────────────┘
```

## 六、正式迁移前必须完成的 PoC

### 6.1 PoC 目标

PoC 只验证“这件事是否值得继续”，不追求完整功能，不迁移全部 UI。

### 6.2 PoC 范围

必须验证以下 6 项：

1. **主窗口启动**
   - 能加载 React 页面
   - 开发模式下可热更新或至少可快速重启

2. **双向通信模型**
   - 渲染层可调用主进程方法
   - 主进程可主动推送事件给渲染层

3. **无边框窗口与标题栏控制**
   - 最小化
   - 最大化 / 还原
   - 关闭
   - 拖拽区域

4. **托盘与菜单栏模式**
   - 托盘图标显示
   - 点击托盘切换主窗口显隐
   - 可从托盘触发取色命令

5. **屏幕取色主链路**
   - 至少在 macOS 上可完成交互式取色
   - 能返回颜色值到 UI

6. **全局快捷键**
   - 至少验证一个前景色取色快捷键
   - 若不可行，必须明确产品是否接受降级

### 6.3 PoC 验收标准

PoC 通过要求：

- 6 项中至少 5 项成立
- 其中“屏幕取色主链路”必须成立
- 主进程与渲染层之间的事件推送必须成立
- macOS 构建产物可运行

### 6.4 PoC 输出物

PoC 结束后必须产出：

- API 验证记录
- 风险复盘
- 是否继续迁移的明确结论
- 若继续，补充正式迁移任务拆解

## 七、Electrobun API 与实现约束

### 7.1 所有 Electrobun 专有能力必须标注状态

文档中凡是涉及以下内容，必须在实施前确认：

- `BrowserWindow`
- `Tray`
- WebView 通信 API
- 静态资源协议，如 `views://`、`resources://`
- 原生绑定能力
- 构建产物组织方式
- 自动更新能力

如果没有官方文档、示例项目或本地 PoC 证明，就不能在实施文档中写成既成事实。

### 7.2 通信模型不能只设计 `call`

现有项目需要三种通信语义：

| 通信类型 | Electron 当前方式 | 迁移后要求 |
| --- | --- | --- |
| 请求 / 响应 | `ipcRenderer.invoke` | 需要等价 `rpc.call` |
| 主进程广播事件 | `webContents.send` | 需要 event push 机制 |
| 安全暴露 API | preload `window.api` | 需要受控 bridge 层 |

建议迁移后仍保留统一的 `window.api` 适配层，而不是在 React 组件中直接散落调用底层 RPC。

## 八、数据迁移与配置存储

### 8.1 现状

当前项目使用 `electron-store` 管理配置，正式迁移时必须处理：

- 配置文件路径
- key 命名
- 默认值
- schema 版本
- 从 Electron 版本到 Electrobun 版本的首次迁移

### 8.2 建议方案

- 使用 JSON 文件存储
- 增加 `schemaVersion`
- 启动时执行一次迁移逻辑
- 迁移前自动备份旧数据
- 迁移失败不删除旧数据

### 8.3 最低要求

必须明确以下内容后才能开工：

- 旧配置文件路径
- 新配置文件路径
- 配置字段映射表
- 迁移成功标记
- 回滚策略

## 九、安全模型与暴露边界

迁移后不能因为没有 Electron preload 就放松边界控制。

### 9.1 原则

- 渲染层不直接访问文件系统
- 渲染层不直接访问原生模块
- 不暴露任意命令执行能力
- 不在 React 组件中直接调用底层运行时 API

### 9.2 建议的暴露层

统一提供：

- `window.api.settings.*`
- `window.api.window.*`
- `window.api.color.*`
- `window.api.events.on(...)`

所有入参都做类型校验，所有对文件、窗口、原生模块的访问都通过白名单方法完成。

## 十、原生取色器策略

这是本项目迁移中风险最高的部分，必须单独看待。

### 10.1 基本判断

当前不应直接承诺“三平台完整原生取色支持”。更现实的路线是：

- **第一阶段只做 macOS PoC**
- macOS PoC 通过后，再评估 Windows
- Linux 尤其是 Wayland 环境单独评估，不纳入首轮承诺范围

### 10.2 平台建议

| 平台 | 建议支持级别 | 说明 |
| --- | --- | --- |
| macOS | P0 | 当前最应该先验证 |
| Windows | P1 | 需处理 DPI 和多显示器 |
| Linux X11 | P2 | 可尝试支持 |
| Linux Wayland | 暂不承诺 | 权限与能力限制较多 |

### 10.3 权限与限制

- **macOS**：如果采用屏幕截图或像素抓取路径，需要验证屏幕录制权限与用户提示流程
- **Windows**：需要验证高 DPI、坐标缩放、多显示器负坐标
- **Linux**：需要先判断是 X11 还是 Wayland，再决定是否允许启用取色能力

### 10.4 原生模块开发建议

原生模块设计可以保留，但应从“完整实现方案”降级为“PoC 方案草图”。

也就是说：

- 当前文档中的 ObjC / C++ / C 示例应视为方向示例
- 不能把这些示例当成已验证可编译方案
- 构建配置必须等 Electrobun 原生扩展机制验证后再固化

## 十一、窗口、托盘与快捷键策略

### 11.1 窗口

当前项目已经实现：

- 无边框窗口
- 自定义标题栏
- 最小化 / 最大化 / 关闭
- 最大化状态同步到渲染层

迁移时必须验证这些不是“能做个窗口”就算完成，而是必须验证：

- 拖拽区域正确
- 自定义控制按钮可用
- 窗口状态变化可推送到前端

### 11.2 托盘

托盘不是附加项，而是产品运行模式的一部分。PoC 至少要验证：

- 托盘图标
- 托盘菜单
- 点击托盘切换主窗口
- menubar 模式下关闭窗口仅隐藏不退出

### 11.3 快捷键

全局快捷键若不可实现，必须作为决策点，而不是默认“以后再说”。

建议：

- 先只验证一个全局快捷键
- 如果只支持应用内快捷键，则明确产品是否接受
- 不要把“菜单快捷键”误当作“全局快捷键”的等价替代

## 十二、项目结构规划（PoC 之后再固化）

原方案中的目录结构可作为参考，但不应在 PoC 前写死为最终结构。

更合理的方式：

```text
peka/
├── poc/
│   └── electrobun/           # 可行性验证代码
├── app/
│   └── electron/             # 当前稳定版本
├── docs/
│   ├── REFACTOR_PLAN.md
│   └── electrobun-poc-report.md
└── ...
```

如果 PoC 通过，再决定是否采用如下正式结构：

```text
src/
├── bun/
├── views/
├── components/
├── stores/
├── native/
└── utils/
```

这样可以避免在验证前就过度投入结构性重组。

## 十三、实施阶段重写

### 阶段 0：可行性验证（1-3 天）

目标：验证 Electrobun 是否适合作为 Peka 的运行时。

交付：

- 最小可运行桌面壳
- 通信 PoC
- 托盘 PoC
- 快捷键 PoC
- macOS 取色 PoC
- 结论报告

### 阶段 1：正式迁移设计（1-2 天）

前提：阶段 0 通过。

工作内容：

- 固化目录结构
- 固化 bridge / RPC 设计
- 固化配置迁移方案
- 补齐签名、打包、发布方案

### 阶段 2：基础能力迁移（2-4 天）

- 窗口管理
- 事件桥接
- 设置存储
- 托盘模式
- 基础 React 承载

### 阶段 3：核心功能迁移（3-5 天）

- 前景色 / 背景色取色
- 颜色格式化与复制
- 状态管理适配
- 主进程事件同步

### 阶段 4：增强能力迁移（2-4 天）

- 自定义标题栏细节
- 辅助功能面板
- menubar 细节行为
- 置顶 / 隐藏任务栏逻辑

### 阶段 5：打包、签名与发布（2-5 天）

- macOS 签名与公证
- Windows 签名（如需要）
- CI 调整
- 安装包验证

### 阶段 6：回归测试与是否切换默认实现（2-4 天）

- 与 Electron 版本逐项对照
- 决定是否切换主分支默认桌面实现

## 十四、时间估算（修正版）

原方案按小时估算过于乐观，尤其低估了原生能力、发布链路与跨平台差异。

### 14.1 更现实的时间范围

| 阶段 | 预计时间 |
| --- | --- |
| 阶段 0：PoC | 1-3 天 |
| 阶段 1：正式设计 | 1-2 天 |
| 阶段 2：基础能力迁移 | 2-4 天 |
| 阶段 3：核心功能迁移 | 3-5 天 |
| 阶段 4：增强能力迁移 | 2-4 天 |
| 阶段 5：打包与发布 | 2-5 天 |
| 阶段 6：回归测试与修复 | 2-4 天 |
| **总计** | **13-27 天** |

### 14.2 为什么不再按小时估算

因为下面这些因素很难在实施前精确按小时预测：

- Electrobun API 成熟度
- 原生绑定是否一次打通
- macOS 权限与签名链路
- Windows DPI / 多显示器问题
- WebView 与 Chromium 的行为差异
- 打包和 CI 环境问题

## 十五、风险评估（修正版）

### 15.1 高风险

| 风险 | 影响 | 应对策略 |
| --- | --- | --- |
| 屏幕取色能力无法稳定实现 | 核心功能失效 | 先做 macOS PoC，失败即停止迁移 |
| 全局快捷键不可用 | 主流程降级 | PoC 单独验证，不成立则作为 Go/No-Go 项 |
| 通信模型无法覆盖事件推送 | 现有架构需重写 | 先验证 event bridge，而非只验证 `call` |
| Electrobun API 不稳定 | 迁移成本失控 | 锁定版本，仅基于已验证能力设计 |
| 打包 / 签名 / 公证链路不可用 | 无法发布 | 在正式迁移前尽早验证产物链路 |

### 15.2 中风险

| 风险 | 影响 | 应对策略 |
| --- | --- | --- |
| WebView 与 Chromium 行为差异 | 样式或交互偏差 | 提前验证关键页面和标题栏交互 |
| 托盘行为跨平台差异 | menubar 模式不一致 | 先以 macOS 为主验证 |
| 设置迁移失败 | 用户配置丢失风险 | 备份旧数据，增加版本化迁移 |
| 原生模块构建复杂 | CI 和本地环境不稳定 | 最早建立最小可编译示例 |

### 15.3 低风险

| 风险 | 影响 | 应对策略 |
| --- | --- | --- |
| 纯 React 展示组件迁移 | 低 | 后移处理 |
| Zustand 状态迁移 | 低 | 保持 store 适配层 |
| CSS 迁移 | 低到中 | 按页面回归验证 |

## 十六、验收标准（正式迁移）

正式迁移完成后，应满足以下 Go / No-Go 标准：

### 16.1 功能标准

- 前景色 / 背景色取色可用
- 颜色复制与格式切换可用
- 设置持久化可用
- 托盘 / menubar 模式可用
- 窗口控制与状态同步可用
- 主进程事件推送链路可用

### 16.2 性能标准

- 冷启动时间优于当前 Electron 版本
- 安装包体积显著小于当前 Electron 版本
- 常驻内存占用优于当前 Electron 版本

### 16.3 稳定性标准

- 连续使用 30 分钟无崩溃
- 取色流程连续执行无明显状态异常
- 设置重启后可正确恢复

### 16.4 发布标准

- macOS 产物可签名并运行
- 若计划分发，则公证流程可跑通
- CI 可稳定产出构建结果

## 十七、建议执行顺序

1. **先做 PoC，不直接迁移**
2. **PoC 优先验证 macOS**
3. **确认 Go / No-Go 后再定正式目录结构**
4. **正式迁移时先迁移基础能力，再迁移 UI**
5. **始终保留 Electron 版本作为回退方案**

## 十八、明确决策点

在正式进入迁移前，必须确认以下问题：

1. **产品是否接受先只支持 macOS 的 Electrobun 版本？**
2. **全局快捷键如果无法实现，是否接受降级？**
3. **设置页是否必须保留独立窗口？**
4. **自动更新是否纳入首版迁移范围？**
5. **Linux 是否明确不作为首轮目标平台？**

## 十九、下一步建议

建议下一步不是“开始全面迁移”，而是：

1. 创建 `poc/electrobun` 最小验证项目
2. 逐项验证窗口、通信、托盘、快捷键、取色
3. 写出 `docs/electrobun-poc-report.md`
4. 基于 PoC 结果再决定是否继续

---

**执行建议：先进行 Electrobun 可行性验证，而不是直接启动完整重构。**
